# AegisrToken Frontend Environment Variables (Optional)
# In production on Vercel, leaving these unset defaults to same-origin /api/* routes.

# Optional: Upstream FastAPI agent server URL (server-side only)
AEGIS_BACKEND_URL=

# Optional: Client-side API URL override (leave empty for same-origin relative URLs)
NEXT_PUBLIC_AEGIS_API_URL=
